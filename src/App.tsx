import React, { useState } from 'react';
import Input, { InputProps } from './Input'

const App: React.FC = () => {

   return (
   <>
       <div className="container">
         <Input />
       </div>
   </>
   );
};

export default App;
